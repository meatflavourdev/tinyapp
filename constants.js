// constants.js
const PORT = 3000; // default port 8080
const USER_ID_LENGTH = 9;
const SHORTID_LENGTH = 6;
const COOKIE_EXPIRE_MINS = 60;

module.exports = { PORT, USER_ID_LENGTH, SHORTID_LENGTH, COOKIE_EXPIRE_MINS };
